bin/
obj/
*.user
.vs/
# runtime media/clip directories (anchored under config/ so the Media/
# source folder, which matches case-insensitively on Windows, stays tracked)
/config/media/
/config/clips/
# runtime-added mounts (managed by the dashboard / control API)
/config/mounts.json
/mounts.json
